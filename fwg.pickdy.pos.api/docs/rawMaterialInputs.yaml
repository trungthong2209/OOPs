paths:
  /rawMaterialInputs/:                # path of the user from your endpoint
    get:                 # endpoint request type (post request)
      tags:               # Tag property
        - RawMaterialInput            # Value of the tag
      summary: get all RawMaterialInputs
      produces:
       - application/json 
      responses:          # server responses
        201:
          description: Get All RawMaterialInput
        401:
          description: Unauthorized            
    post:                 # endpoint request type (post request)
      tags:               # Tag property
        - RawMaterialInput            # Value of the tag
      summary: creates a new RawMaterialInput
      produces:
      - application/json
      parameters:         # request parameters
      - in: body          # request body
        name: createShop     # name of request, can be any name
        description: It enables a user to create an RawMaterialInput
        required: false   # can also be true depending on user preference
        schema:           # Schema definition
          $ref: '#/definitions/RawMaterialInputInsert' 
      responses:          # server responses
        201:
          description: An object with RawMaterialInput details
        401:
          description: Unauthorized  
    put:                 # endpoint request type (post request)
      tags:               # Tag property
        - RawMaterialInput            # Value of the tag
      summary: update RawMaterialInput
      produces:
       - application/json
      parameters:         # request parameters
      - in: body          # request body
        name: updateShop     # name of request, can be any name
        description: It enables update RawMaterialInput
        required: false   # can also be true depending on user preference
        schema:           # Schema definition
          $ref: '#/definitions/RawMaterialInputUpdate' 
      responses:          # server responses
        201:
          description: An object with RawMaterialInput details
        401:
          description: Unauthorized 
    delete:                 # endpoint request type (post request)
      tags:               # Tag property
        - RawMaterialInput            # Value of the tag
      summary: delete RawMaterialInput
      produces:
       - application/json
      parameters:         # request parameters
      - in: body          # request body
        name: rawMaterialInputDelete     # name of request, can be any name
        description: true if delete sucessfully
        required: false   # can also be true depending on user preference
        schema:           # Schema definition
          $ref: '#/definitions/RawMaterialInputDelete' 
      responses:          # server responses
        201:
          description: An object with RawMaterialInput details
        401:
          description: Unauthorized 
  /rawMaterialInputs/{id}:     
    get:                 # endpoint request type (post request)
      tags:               # Tag property
        - RawMaterialInput            # Value of the tag
      summary: get all RawMaterialInputs by Id
      produces:
       - application/json 
      parameters:     # request parameters
      - name: id      # name of parameter passed
        in: path      # parameters in the path
        description: path parameter takes the id
        required: true
        type: string
      responses:          # server responses
        201:
          description: Get All RawMaterialInput
        401:
          description: Unauthorized             

definitions:        # Schema defination for request body
  RawMaterialInputInsert:
    type: object
    properties:      
      amount:
        type: number
      rawMaterialId:
        type: string
      shopId:
        type: string
      createByUserId:
        type: string
      updateByUserId:
        type: string
      createDateTime:
        type: string
      updateDateTime:
        type: string
      reason:
        type: string
      actionType:
        type: string
  RawMaterialInputUpdate:
    type: object
    properties:
      criteria:
        type: object
        properties:
          _id:
            type: string
      data:
        type: object
        properties:      
          _id:
            type: string
          amount:
            type: number
          rawMaterialId:
            type: string
          shopId:
            type: string
          createByUserId:
            type: string
          updateByUserId:
            type: string
          createDateTime:
            type: string
          updateDateTime:
            type: string
          reason:
            type: string
          actionType:
            type: string
  RawMaterialInputDelete:
    type: object
    properties:          
      _id:
        type: string          