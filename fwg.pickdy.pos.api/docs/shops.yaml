paths:
  /shops/:                # path of the user from your endpoint
    get:                 # endpoint request type (post request)
      tags:               # Tag property
        - Shop            # Value of the tag
      summary: get all Shops
      produces:
       - application/json 
      responses:          # server responses
        201:
          description: Get All Shop
        401:
          description: Unauthorized            
    post:                 # endpoint request type (post request)
      tags:               # Tag property
        - Shop            # Value of the tag
      summary: creates a new Shop
      produces:
      - application/json
      parameters:         # request parameters
      - in: body          # request body
        name: createShop     # name of request, can be any name
        description: It enables a user to create an Shop
        required: false   # can also be true depending on user preference
        schema:           # Schema definition
          $ref: '#/definitions/shopInsert' 
      responses:          # server responses
        201:
          description: An object with Shop details
        401:
          description: Unauthorized  
    put:                 # endpoint request type (post request)
      tags:               # Tag property
        - Shop            # Value of the tag
      summary: update Shop
      produces:
       - application/json
      parameters:         # request parameters
      - in: body          # request body
        name: updateShop     # name of request, can be any name
        description: It enables update Shop
        required: false   # can also be true depending on user preference
        schema:           # Schema definition
          $ref: '#/definitions/shopUpdate' 
      responses:          # server responses
        201:
          description: An object with Shop details
        401:
          description: Unauthorized 
    delete:                 # endpoint request type (post request)
      tags:               # Tag property
        - Shop            # Value of the tag
      summary: delete Shop
      produces:
       - application/json
      parameters:         # request parameters
      - in: body          # request body
        name: shopDelete     # name of request, can be any name
        description: It enables delete Shop
        required: false   # can also be true depending on user preference
        schema:           # Schema definition
          $ref: '#/definitions/shopDelete' 
      responses:          # server responses
        201:
          description: true if delete sucessfully
        401:
          description: Unauthorized 
  /shops/genQrCode:     
    put:                 # endpoint request type (post request)
      tags:               # Tag property
        - Shop            # Value of the tag
      summary: genQrCode
      produces:
       - application/json
      parameters:         # request parameters
      - in: body          # request body
        name: updateShop     # name of request, can be any name
        description: It enables gen QrCOde
        required: false   # can also be true depending on user preference
        schema:           # Schema definition
          $ref: '#/definitions/shopUpdate' 
      responses:          # server responses
        201:
          description: An object with Shop details
        401:
          description: Unauthorized  
  /shops/{id}:     
    get:                 # endpoint request type (post request)
      tags:               # Tag property
        - Shop            # Value of the tag
      summary: get Shop by Id
      produces:
       - application/json 
      parameters:     # request parameters
      - name: id      # name of parameter passed
        in: path      # parameters in the path
        description: path parameter takes the id
        required: true
        type: string
      responses:          # server responses
        201:
          description: Get All Shop
        401:
          description: Unauthorized                     

definitions:        # Schema defination for request body
  shopInsert:
    type: object
    properties:     
      subscriptionId:
        type: string
      name: 
        type: string
      orginizationId: 
        type: string
      parentLocationId: 
        type: string
      locationId: 
        type: string
      qrcode: 
        type: string      
      regions: 
        type: array
        items:
          type:string
  shopUpdate:
    type: object
    properties:
      criteria:
        type: object
        properties:
          _id:
            type: string
      data:
        type: object
        properties:     
          _id:
            type: string
          subscriptionId:
            type: string
          name: 
            type: string
          orginizationId: 
            type: string
          parentLocationId: 
            type: string
          locationId: 
            type: string
          qrcode: 
            type: string  
          regions: 
            type: array
            items:
              type:string
  shopDelete:
    type: object
    properties:          
      _id:
        type: string